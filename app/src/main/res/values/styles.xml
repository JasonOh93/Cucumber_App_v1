<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary_my</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark_my</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- IntroActivity theme. -->
    <style name="AppTheme.IntroActivity" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary_my</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark_my</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- MainActivity theme. -->
    <style name="AppTheme.MainActivity" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary_my</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark_my</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Location theme. -->
    <style name="AppTheme.LocationActivity" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary_my</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark_my</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

</resources>
